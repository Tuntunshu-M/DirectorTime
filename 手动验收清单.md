# 导演时间 · 手动验收清单

> 用法：**第 0 节 5 分钟先跑完**，其余按需挑。每项都写了「怎么做 → 看哪里」，不用凭感觉。
> 不通过的把那行的编号发我（例如「4.3 不通过，注入里没有冷场句」）。
>
> - **面板** = 酒馆扩展菜单里的「导演时间」
> - **配置页** = 面板右上角「配置」
> - **控制台** = F12 → Console
> - **Debug** = 面板上的「打开调试面板」

---

## 0. 五分钟冒烟（先跑这 10 项）

| # | 怎么做 | 期望 |
|---|---|---|
| 0.1 | 面板底部看版本号 | 显示当前版本（如 `v0.3.1`） |
| 0.2 | 打开总开关 | 开关变开，Debug 顶部「启用」为是 |
| 0.3 | 配置页 → 测试连接 | 绿（失败会写原因） |
| 0.4 | 面板 → 生成剧本 | 出现场次；Debug「大纲 / 场次」有内容 |
| 0.5 | 随便发一句正常对话 | Debug「注入」非空，且**不带** `[导演指令]` 之外的乱码 |
| 0.6 | 发一句明确反对（例：**我不要去那里**） | Debug「上一轮判定」的 `action` 是 `regenAfter` / `drop` / `rewrite` 之一（不是 `advance`） |
| 0.7 | 看 Debug 的「主动性」 | 是「如果冷场，你就**你自己的侧写推出来的事**」，不是通用句「自己找一件事继续」 |
| 0.8 | 关掉总开关 | Debug「注入」立刻变空（不是等下一轮） |
| 0.9 | 换到另一个聊天窗口 | 剧本与注入都不残留；回来还是原样 |
| 0.10 | 面板 → 更新插件 | 控制台出现 `[导演时间] 更新成功 { name: …, global: … }`；失败会写「试过的路径」 |

---

## 1. 一键更新（T-420）

| # | 怎么做 | 期望 |
|---|---|---|
| 1.1 | 点「更新插件」 | 成功后**页面自动刷新**，版本号变新 |
| 1.2 | 故意让更新失败（断网再点） | 面板写「更新失败（原因）…请到「扩展」面板手动点更新」，**页面不刷新** |
| 1.3 | 更新成功后**不要**自己按 F5 | 页面已经自动刷过；不会来回刷第二次 |
| 1.4 | 不点按钮，直接在酒馆「扩展」面板点 Update | 15 秒内本插件发现版本变化并自动刷新，提示「已更新到 vX，正在刷新页面」 |

> 1.1 / 1.2 失败时，请把控制台那两行发我：
> `[导演时间] 更新成功 {…}` 或 `[导演时间] 一键更新失败，试过的路径：[…]`

---

## 2. 生成与侧写（T-201 / T-204 / T-402）

| # | 怎么做 | 期望 |
|---|---|---|
| 2.1 | 配置页改 API（端点 / 密钥 / 模型）→ 保存 → 测试连接 | 能连通；Debug 成本行开始计数 |
| 2.2 | 生成剧本 | 大纲含主线目标（`objective`）；场次 2~4 条、每条有 criteria / antiCriteria |
| 2.3 | 面板 → 「重新续写」 | 追加新场次，已演过的不重复 |
| 2.4 | 配置页 → 人物侧写 | 自动生成过；能手动改；改完有「锁」标记 |
| 2.5 | 改一个侧写字段（例：核心欲望）→ 发一句话 | 下一轮注入里的**冷场句换成按新侧写推的**（同一版侧写只重生成一次） |
| 2.6 | 换一张角色卡 | 侧写跟着换角色卡走（不是跟着聊天） |

---

## 3. 破限与预设（T-411 / T-418）⚠️ 有坑

| # | 怎么做 | 期望 |
|---|---|---|
| 3.1 | 配置页 → 预设 → 选一个酒馆预设 | 列得出来；选了之后显示条目 |
| 3.2 | 只做 3.1，然后发一句话 | **不会**真的注入 —— 默认 `breakFilter.mode = 'off'`，开关没开 |
| 3.3 | 控制台开模式后再发一句话 | 导演请求的第一条 system 前面出现预设内容 |

```js
// 3.3 用：让"选中的酒馆预设"真正生效
DirectorTime.api.breakFilter.set({ mode: 'preset' })   // 可选 preset / custom / append / off
DirectorTime.api.breakFilter.get()                      // 看当前模式
```

> ⚠️ 这是这次自查发现的坑：Debug 的「破限」行会写「预设「X」生效 N 字」，但 `mode=off` 时**并不真注入**。
> 以行为为准。要不要把 Debug 文案改成"已选但模式关闭"，你说一声我就改。

---

## 4. 每轮注入与节奏（T-203 / T-416 / T-417）

| # | 怎么做 | 期望 |
|---|---|---|
| 4.1 | 正常演一楼 | 注入含：本场目标 / 走位 / 冷场句；**不出现** `[导演指令]` 原文被角色复述 |
| 4.2 | 连续演到接近上限（默认 8 楼） | 注入变成「这一场够久了，别再问了，直接做一件事推下去」 |
| 4.3 | 让本场达成 | 本场变「已达成、收尾中」，注入换收尾话术（不重复询问） |
| 4.4 | 第 1 楼就达成 | 立刻进 ready，**不被 min=3 压着不切场** |
| 4.5 | 只回「嗯」「哦」演 3 轮 | 卡住计数到 3 → 强制推进，不死锁 |
| 4.6 | 每轮结束看 Debug | 「回合」+1；本场 `turnCount` +1 |

---

## 5. 用户意愿与强制爱（T-405 / 原 T-419）

| # | 怎么做 | 期望 |
|---|---|---|
| 5.1 | 默认（will=80）说「我不要去那儿」 | 让步：本场作废（`dropped`）或重生成后续，`action ≠ advance` |
| 5.2 | `saveSettings({ will: 10 })` 再说反对 | 先 `rewrite`（换个说法再试一次）；**再拒一次**才 `drop` |
| 5.3 | `saveSettings({ will: 50 })` 再说反对 | 直接 `drop` |
| 5.4 | 说无关话（「今天天气不错」） | 高档：暂停（注入被清空）；低/中档：跟一两楼再拉回 |
| 5.5 | 强制爱开启后再说反对 | **不让步**：本场不换不丢，继续注入；卡住 3 轮才强制推进 |
| 5.6 | 强制爱 + 明确触及硬禁区 | **仍然让步**（硬禁区优先，`violated` 为真） |

```js
DirectorTime.store.saveSettings({ will: 50 })              // 0~33 剧情优先 / 34~66 平衡 / 67~100 user 优先
DirectorTime.store.saveSettings({ forceAffection: true })   // 强制爱（默认关）
DirectorTime.store.saveSettings({ forceAffection: false })
```

---

## 6. 档位与待确认队列（T-414）

| # | 怎么做 | 期望 |
|---|---|---|
| 6.1 | 配置页 → 档位改成 L0 / L1 / L2 | 逐功能点独立生效（生成大纲 / 分场 / 侧写 / 态度判定 / 推进点判定 / 一致性） |
| 6.2 | 某功能设 L1，然后触发它 | 不直接生效，进面板「待确认」块 |
| 6.3 | 面板点「采用」/「丢弃」 | 采用才写入；丢弃什么都不变 |
| 6.4 | 某功能设 L0 | 完全不调那一步（Debug 里能看到没发生） |

---

## 7. 投机执行（T-407）

| # | 怎么做 | 期望 |
|---|---|---|
| 7.1 | 正常演几楼 | Debug「投机」行显示 命中 / 失手 + 命中率；命中时下一轮几乎零延迟 |
| 7.2 | 关掉投机 | 不再预演；每轮少一次 API 调用（成本行看得出） |

```js
DirectorTime.store.saveSettings({ speculation: false })
```

---

## 8. 目前只有控制台入口的功能（这次自查刚接通）

> 这些功能此前 `api` **没有挂到 window**，控制台其实够不着 —— 现在修好了，下面这些能直接用。

| # | 功能 | 命令 | 期望 |
|---|---|---|---|
| 8.1 | 剧情占比（T-415） | `DirectorTime.api.tone.get()` / `.set('daily', 60)` | 三条线联动配平、和恒为 100；生成的书纲随之变 |
| 8.2 | 主角设置（T-412 多人卡） | `DirectorTime.api.cast.set(['甲','乙'])` | 注入只写当前说话人视角；不是他的戏不注入 |
| 8.3 | 伏笔（T-408） | `DirectorTime.api.foreshadows.list()` / `.resolve(id)` | 列出待回收；手动销账后 Debug 伏笔行减少 |
| 8.4 | 副本迁移（T-413） | `DirectorTime.api.copy.export()` / `.preview(c)` / `.import(c)` | 副本**不含端点与密钥**；导入前有确认弹窗 |

---

## 9. 生命周期与兜底（T-204）

| # | 怎么做 | 期望 |
|---|---|---|
| 9.1 | 关总开关 | 立刻清空注入（不是等下一轮） |
| 9.2 | 换聊天窗口 | 剧本 / 注入 / 世界书勾选都不串（世界书勾选是聊天级） |
| 9.3 | 换角色卡 | 侧写跟着角色走 |
| 9.4 | 面板 → 清空剧本 | 注入清空，点「生成剧本」能重开 |
| 9.5 | API 故意填错 / 断网 | 报错但**不影响角色正常回复**；下一轮能恢复 |
| 9.6 | 演满 15 轮（默认上限） | 提示「已超过 15 轮，剧本已清空」 |

---

## 附：默认开关一览（省得你猜现在是什么状态）

| 项 | 默认 | 含义 |
|---|---|---|
| `enabled` | **关** | 总开关，要先打开 |
| `injectEnabled` | 开 | 注入开关 |
| `will` | 80 | user 优先档 |
| `forceAffection` | 关 | 强制爱 |
| `pacing` | 3 ~ 8 | 每场最少 3 楼、最多 8 楼 |
| `stuckThreshold` | 3 | 卡住几轮强制推进 |
| `confidenceThreshold` | 0.7 | 低于此置信按"接受"处理 |
| `maxRounds` | 15 | 超过就清空剧本 |
| `consistencyCheck` | 开 | 生成后一致性自检 |
| `speculation` | 开 | 投机执行（每轮多一次调用） |
| `breakFilter.mode` | **off** | 破限词模式 —— 选预设也没用，见第 3 节 |
| `preset.name` | 空 | 未选酒馆预设 |
| `hardLimits` | 空 | 硬禁区（命中即停） |
| `protagonists` | 空 | 单卡老行为 |
| `objective` | 空 | 留空由 AI 自己构思主线目标 |
| 档位 | 大纲/分场/侧写 = L1，态度判定/推进点判定/一致性 = L2 | L0 全手动 / L1 待确认 / L2 全自动 |
